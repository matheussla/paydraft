# Core

Application setup, configuration, and routing.

## Structure

- `App.tsx` - Root component
- `main.tsx` - Application entry point
- `router/` - Route definitions (future)
- `providers/` - Context providers (future: Auth, Theme, etc.)

## Rules

- Minimal logic, mostly composition
- Imports features and wires them together
- Global configuration only
